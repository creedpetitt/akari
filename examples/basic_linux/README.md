# Akari Linux Example

This directory contains a standard Linux/macOS example of the Akari web framework.

## Quick Start

Run the following commands to build the server and run it on port 8080:

```bash
make
./akari_linux_demo
```
